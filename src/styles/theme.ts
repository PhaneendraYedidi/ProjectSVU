export const lightColors = {
  primary: '#3498db', // A vibrant blue for primary actions
  secondary: '#2ecc71', // A calming green for success states
  accent: '#f39c12', // An orange for highlights and interactive elements
  background: '#ecf0f1', // A light gray for general background
  surface: '#ffffff', // White for cards and surfaces
  textPrimary: '#2c3e50', // A dark blue for primary text
  textSecondary: '#7f8c8d', // A medium gray for secondary text
  disabled: '#95a5a6', // A light gray for disabled elements
  error: '#c0392b', // A red for error states
  success: '#27ae60', // A green for positive feedback
  border: '#bdc3c7', // A light gray for borders and dividers
};

export const darkColors = {
  primary: '#89c4f4', // A lighter blue for primary actions in dark mode
  secondary: '#58d68d', // A brighter green for success states in dark mode
  accent: '#f8c471', // A lighter orange for highlights in dark mode
  background: '#34495e', // A dark blue for general background in dark mode
  surface: '#2c3e50', // A darker blue for cards and surfaces in dark mode
  textPrimary: '#ffffff', // White for primary text in dark mode
  textSecondary: '#ecf0f1', // A light gray for secondary text in dark mode
  disabled: '#7f8c8d', // A medium gray for disabled elements in dark mode
  error: '#e74c3c', // A red for error states in dark mode
  success: '#2ecc71', // A green for positive feedback in dark mode
  border: '#34495e', // A dark blue for borders and dividers in dark mode
};

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

export const typography = {
  h1: {
    fontSize: 28,
    fontWeight: 'bold',
    letterSpacing: -0.5,
  },
  h2: {
    fontSize: 22,
    fontWeight: '600',
    letterSpacing: -0.25,
  },
  body: {
    fontSize: 16,
    lineHeight: 24,
    letterSpacing: 0.15,
  },
  button: {
    fontSize: 18,
    fontWeight: '500',
    letterSpacing: 0.1,
  },
  caption: {
    fontSize: 12,
    letterSpacing: 0.4,
  },
};